# Audio-AI Production Environment Configuration
# Copy this file to .env.production and fill in your values

# =============================================================================
# REQUIRED CONFIGURATION
# =============================================================================

# Gemini AI API Key (REQUIRED)
GEMINI_API_KEY=your_gemini_api_key_here

# =============================================================================
# SERVICE CONFIGURATION  
# =============================================================================

# Backend service port (default: 3000)
BACKEND_PORT=3000

# Transcription Provider (openai_whisper_webservice or docker_whisper)
TRANSCRIPTION_PROVIDER=openai_whisper_webservice

# Whisper Service URL (internal Docker network)
WHISPER_SERVICE_URL=http://whisper:9000

# =============================================================================
# FILE HANDLING CONFIGURATION
# =============================================================================

# Maximum file size in MB
MAX_FILE_SIZE_MB=100

# Host path for processed files (absolute path on host machine)
PROCESSED_FILES_PATH=/var/lib/audio-ai/processed

# =============================================================================
# SYSTEM CONFIGURATION
# =============================================================================

# Application logging level (error, warn, info, debug)
LOG_LEVEL=info

# System timezone
TZ=UTC

# =============================================================================
# FILE PERMISSIONS CONFIGURATION
# =============================================================================

# User ID and Group ID for file permissions (run `id` command to get your values)
# This ensures the container can write files with correct ownership
PUID=1001
PGID=1001

# =============================================================================
# PORTAINER CONFIGURATION NOTES
# =============================================================================
# When deploying through Portainer:
# 1. Set PROCESSED_FILES_PATH to a persistent host directory
# 2. Ensure GEMINI_API_KEY is securely stored in Portainer secrets
# 3. Update the GitHub image reference in docker-compose.production.yml
# 4. Configure Portainer webhooks for automatic redeployment on image updates