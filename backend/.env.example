# Copy this file to .env and add your actual API keys

# Required: Gemini API key for AI processing
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: Transcription provider (defaults to free_web_speech)
TRANSCRIPTION_PROVIDER=free_web_speech

# Optional: API key for transcription service (only needed for some providers)
# TRANSCRIPTION_API_KEY=your_transcription_api_key_here

# Optional: Custom output directory (defaults to saved_ideas)
# BASE_DIRECTORY=saved_ideas

# Optional: Custom port (defaults to 3000)
# PORT=3000

# Optional: Whisper service URL (for docker_whisper provider)
# WHISPER_SERVICE_URL=http://localhost:8001